# 学习闭环二期功能

## 目标

在课程播放器重构之外，补齐"学后留存"的三个轻量功能：生词本、场景搜索、学习记录页，形成学习闭环的复习端。

## 范围

- 包含：生词本（查词沉淀 + 列表页 + 来源场景跳转）、首页场景关键词搜索、学习记录页（连续天数 / 总时长 / 完成数 / 打卡日历）。
- 不包含：间隔复习算法（先靠生词本与难句标记的手动复习）、跨端同步策略变更。

## 背景

- 相关文档：`docs/PRODUCT_SENSE.md`、`docs/FRONTEND.md`（首屏不展示统计卡的约定——学习记录做独立页面不违反）。
- 相关代码路径：`miniprogram/utils/api.ts`（`wordLookupCache` 已缓存查词结果 7 天；`UserProgress` 已含 `streakCount/totalCompleted`；`UserProfile.studySeconds`）、`miniprogram/pages/index/index.ts`（章节数据已全量在前端，搜索可纯前端过滤）。
- 已知约束：生词本起步用本地存储即可，登录用户同步后端需要新表（后端仓库配合项）；打卡日历需要后端按日聚合学习记录（`waimao_mini_study_sessions` 已存在，需确认查询接口）。

## 风险

- 风险：功能分散导致首页入口拥挤。
- 缓解：生词本与学习记录都收进"个人卡"点击后的页面，首页不加新平铺入口；搜索用首页列表顶部轻量搜索框。

## 里程碑

1. 复习入口（生词本 + 难句合并）：查词时写入本地生词表（词、释义、来源 sceneId）；难句星标（里程碑 3 已落地 `utils/practice-marks.ts` / `waimao_starred_cues_v1`）汇入同一复习页；支持"生词/难句"分组浏览、点击回源句、课程页"只播标记的句子"。
2. 场景搜索：首页标题/tag 关键词过滤，命中直达小节。
3. 学习记录页：现有字段展示 + 后端按日聚合接口 + 打卡日历；练习次数作为数据源之一（当前 `practiceCounts` 仅内存，需落存储或上报才能长期统计"每句练过几遍"）。

## 验证方式

- 命令：`npm run typecheck && npm test`（生词本存取与搜索过滤的纯逻辑测试）。
- 手工检查：查词→生词本→回源句链路；难句复习"只播标记句"；搜索命中锁定小节时的解锁引导；学习记录与后端数据一致。

## 进度记录

- [x] 里程碑 1：复习入口（生词本 + 难句）。
- [x] 里程碑 2：场景搜索。
- [x] 里程碑 3：学习记录页。

## 决策记录

- 2026-07-11：建档。难句标记与录音对比放在课程播放器重构计划内，本计划只做"学后"侧。
- 2026-07-12：难句星标已在播放器重构里落地但"只存不用"，本计划里程碑 1 将其与生词本合并为统一复习入口；练习次数长期统计归入里程碑 3。
- 2026-07-12：用户决定暂缓阶段 C，直接开始阶段 D。入口收敛为“首页个人卡 → 学习主页”，学习主页承载学习记录与复习入口，避免在首页继续平铺功能；生词与难句第一期保持本地存储，学习时长、练习次数和按日记录走小程序专用后端。
- 2026-07-12：阶段 D 代码完成。开发者工具确认首页搜索能筛出 Cold Call、个人卡可进入学习主页、复习空态页面可达；线上后端尚未部署新增学习记录接口，因此体验版真机 smoke 留到部署后执行。
