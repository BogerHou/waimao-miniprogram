# 外贸英语影子跟读首发清单

## 发布基线

- 发布分支：`main`
- 小程序 appid：以 `project.config.json` 为准
- 建议版本号：`1.0.0`
- 建议版本描述：`首发：7 章外贸英语场景，支持双语文本、知识点、查词和复习。`
- 当前发布模式：线上 `interactiveFeaturesEnabled=false`；7 章均可阅读，会员解锁、音频播放、录音和相关文案暂不开放。
- 实验分支 `codex/scenario-speaking-coach` 不合并、不上传。

## 自动门禁

上传前在小程序仓库执行：

```sh
npm run release:check
```

该命令覆盖类型检查、全量测试、正式 appid、线上 API、7 章 50 个场景、第一课字幕与音频、二维码、微信密钥健康状态、匿名权限边界和包体大小。

后端仓库执行：

```sh
npm run build
npm test
```

## 开发者工具上传提示

- [x] 已定位“图片和音频资源大小应不超过 200 K”：`miniprogram/assets/images/icon.png` 为 261,045 字节。该图标仍被联系页和分享功能使用，不是无用文件；当前按用户决定不改代码或资源，作为后续包体质量优化项保留。
- [x] 已确认“无依赖文件”提示与 `project.config.json` 中 `ignoreUploadUnusedFiles=false` 一致。该设置用于避免 TypeScript 编译后的辅助模块被静态依赖分析误删，不能仅为消除提示而直接开启过滤；如需优化，先在开发者工具“点击查看”中保存完整文件清单，再逐项验证。
- 上述两项是代码包质量提示，不代表本轮真机功能回归失败；如果开发者工具仍允许继续上传，可保留提示提交当前体验版。如果工具明确阻止上传，再单独安排资源压缩和依赖清单核对。

## 微信后台

- [x] `request 合法域名`：`https://englishecho.site`
- [x] `downloadFile 合法域名`：`https://englishecho.site`、`https://dict.youdao.com`
- [x] `downloadFile 合法域名`新增：`https://waimao-audio.englishecho.site`
- [x] 小程序类目与英语学习内容相符，并已按主体资质确认可提交审核的教育类目。
- [x] 隐私保护指引已说明头像和昵称仅用于小程序内学习档案；录音只用于本机即时对比，听完即弃、不保存不上传；当前版本不读取位置、通讯录，也不接微信支付。
- [x] 后台音频能力与 `requiredBackgroundModes: ["audio"]` 保持一致。
- [x] 在微信开发者工具重新打开项目刷新配置，并在开启合法域名校验时编译通过；首页成功加载 7 章，控制台错误为 0。

## 体验版验收

- [x] 当前关闭态已完成真机验收（2026-07-13）。
- [x] 匿名首页展示 7 章，所有章节均可直接进入并阅读，无锁定态。
- [x] 首页不再展示交流悬浮框；“我的”页仍可进入交流与反馈。
- [x] 全站不展示会员、解锁、邀请码、付费、播放、发音或录音入口及相关文案。
- [x] 课程正文、双语短句、知识点、场景搜索、查词释义、复习和学习记录可正常使用。
- [x] 复习页回到来源句时只定位原句，不初始化或自动播放音频。
- [x] 收藏生词和难句后重新登录，当前设备上的收藏仍保留；清理小程序数据或更换设备仍不承诺同步。
- [x] 交流与反馈页展示交流群二维码。
- [x] 开启合法域名校验时编译和真机访问正常，控制台无阻断错误。

## 审核说明

建议在审核备注中写明：

> 本小程序用于外贸英语场景学习。当前体验版开放全部 7 章双语正文、知识点、查词、复习与学习记录；会员解锁、邀请码、音频播放和录音入口暂未开放。当前版本不接微信支付。

## 上线与回滚

1. 上传 `1.0.0`，设为体验版；当前关闭态真机验收已于 2026-07-13 完成。
2. 提交微信审核，审核说明必须与当前关闭态一致，不提供邀请码或引导审核员寻找隐藏能力。
3. 审核通过后发布，并在首小时复查 `/api/health`、线上开关、首页 7 章、登录和收藏保全。
4. 若出现阻断问题，在微信版本管理中回退上一正式版本；本次小程序发布不包含数据库迁移。
